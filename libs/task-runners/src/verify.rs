use std::path::PathBuf;

pub async fn verify_data_installation(dataset: Vec<String>, _install_location: PathBuf) {
    for _item in dataset {
        {
            todo!("Match external installations when relevant");
        }
    }
}
