[package]
name = "build"
version = "0.1.0"
authors = ["taylor <taylor@teukka.tech>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
async-recursion = "1.0.0"
chrono = "0.4.19"
directories = "3.0.1"
futures = "0.3.19"
markdown = { path = "../markdown" }
num_cpus = "1.13.0"
persistance = { path = "../persistance" }
render = { path = "../render" }
rpassword = "5.0.1"
serde = "1.0.125"
serde_derive = "1.0.125"
tasks = { path = "../tasks" }
# TODO: remove this in favor of async
threadpool = "1.8.1"
tokio = { version = "1.15.0", features = ["rt", "sync"] }
toml = "0.5.8"
