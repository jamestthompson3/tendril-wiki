[general]
wiki_location = "~/wiki"
port = 6683
user = ""
pass = ""
version = "0.0.1"
# directory where images / pdfs / other files are stored
media_location = "~/wiki_media"

[sync]
use_git = true
# time in seconds that tendril will try and sync changes to git
sync_interval = 60
# branch it will try and push to
branch = "main"
